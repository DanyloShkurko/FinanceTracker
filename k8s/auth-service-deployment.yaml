apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service-ft
  labels:
    app: auth-service-ft
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service-ft
  template:
    metadata:
      name: auth-service-ft
      labels:
        app: auth-service-ft
    spec:
      containers:
        - name: auth-service-ft
          image: danil2110/auth-service-ft
          ports:
            - containerPort: 8082
          env:
            - name: EUREKA_ADDRESS
              valueFrom:
                configMapKeyRef:
                  key: EUREKA_ADDRESS
                  name: eureka-cm

            - name: CONFIG_SERVER_URI
              valueFrom:
                configMapKeyRef:
                  key: config_url
                  name: config-cm

            - name: POSTGRES_HOST
              valueFrom:
                configMapKeyRef:
                  key: hostname
                  name: psql-cm

---

apiVersion: v1
kind: Service
metadata:
  name: auth-service-svc
spec:
  selector:
    app: auth-service-ft
  ports:
    - port: 80
      targetPort: 8082
